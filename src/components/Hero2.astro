---
import { Icon } from 'astro-icon/components';
---

<section class="relative pt-24 pb-32 md:pt-32 md:pb-48 overflow-hidden">
  
  <!-- 背景装饰：一个巨大的、极其模糊的暖色光晕，营造氛围 -->
  <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[1000px] h-[500px] bg-primary/10 rounded-full blur-[120px] pointer-events-none"></div>

  <div class="container max-w-4xl mx-auto px-6 text-center">
    
    <!-- 1. Promo Pill: 替代你的 Banner -->
    {/* 只有有推广时才显示 */}
    <a 
      href="/projects/td2bd" 
      class="inline-flex items-center gap-2 px-3 py-1 rounded-full border border-border/40 bg-muted/50 text-xs font-medium text-muted-foreground hover:bg-muted transition-colors mb-8"
    >
      <span class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-primary"></span>
      </span>
      TD2BD v2.0 is now available
      <span class="text-muted-foreground/50">→</span>
    </a>

    <!-- 2. Avatar: 带一点高级的内发光 -->
    <div class="mb-8">
      <div class="relative mx-auto w-24 h-24 md:w-32 md:h-32 rounded-full overflow-hidden ring-1 ring-border/50 shadow-2xl">
        <img src="/avatar.jpg" class="w-full h-full object-cover" />
      </div>
    </div>

    <!-- 3. Headline: 使用 text-balance 保持平衡 -->
    <h1 class="text-4xl md:text-6xl font-bold tracking-tight text-foreground mb-6 text-balance">
      Hi, I'm Kyle. <br class="hidden md:block" />
      <span class="text-muted-foreground">I build polished software.</span>
    </h1>

    <!-- 4. Subheadline: 你的 Tagline -->
    <p class="text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto mb-10 text-balance leading-relaxed">
      A Full-stack Developer based in Shanghai. 
      Obsessed with <span class="text-foreground font-medium">SaaS Design</span>, 
      <span class="text-foreground font-medium">React Ecosystem</span>, and 
      <span class="text-foreground font-medium">Mechanical Keyboards</span>.
    </p>

    <!-- 5. Tech Stack / Socials -->
    <div class="flex items-center justify-center gap-4 text-muted-foreground">
       <!-- 放置你的 Github, Twitter, 或技术栈图标 -->
       <!-- 样式做成 monochrome (单色)，不要五颜六色，保持克制 -->
       <Icon name="ri:github-fill" class="size-6 hover:text-foreground transition-colors" />
       <Icon name="ri:twitter-fill" class="size-6 hover:text-foreground transition-colors" />
       <div class="w-px h-4 bg-border"></div> <!-- 分割线 -->
       <Icon name="ri:reactjs-fill" class="size-6 hover:text-[#61DAFB] transition-colors" />
       <Icon name="ri:vuejs-fill" class="size-6 hover:text-emerald-500 transition-colors" />
    </div>

  </div>
</section>