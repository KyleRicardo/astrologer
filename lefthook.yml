pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{ts,tsx,js,jsx,astro,html,css,json,md,mdx}"
      run: pnpm format {staged_files}
      stage_fixed: true

    lint:
      glob: "*.{ts,tsx,js,jsx,json,md,mdx,astro}"
      run: pnpm lint:fix {staged_files}
      stage_fixed: true
